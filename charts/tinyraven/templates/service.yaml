apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinyraven.fullname" . }}
  labels:
    {{- include "tinyraven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinyraven.selectorLabels" . | nindent 4 }}
